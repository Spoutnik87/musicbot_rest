spring.datasource.url=jdbc:postgresql://${DB_HOST:192.168.1.29}:5432/${DB_NAME:musicbot}
spring.datasource.platform=postgres
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=create

# File upload
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=20MB
spring.servlet.multipart.location=${FILE_PATH:/usr/share/musicbot/}

server.error.whitelabel.enabled=false

security.secret=${SECURITY_SECRET:Ap3KnAdeSsrAWYzUckczf4VvgP4C57wq8f3XHy6ESdcqucNk}